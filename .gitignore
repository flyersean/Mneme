# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Mneme runtime state — generated at install/setup, never committed
mneme.yaml
chunks/
*.db
faiss.index
*.log

# OS
.DS_Store

# Personal / local dev scratch scripts — hardcode local paths and model names and
# are not part of the distributed tooling. Kept out of the repo so a `git add -A`
# can't sweep them into a release.
scripts/benchmark.py
scripts/gen_doc.py
scripts/personal_test.py
scripts/relaunch_effort.sh
scripts/run_large_input_test.py
scripts/set_hf.py
scripts/weather_test.py
